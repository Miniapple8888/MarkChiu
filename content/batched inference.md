also known as offline inference
generating predictions on a batch of observations on scheduled intervals (hourly, daily)

pros:
- save on latency
drawbacks:
- not available for real time inference

example: netflix generates recommendations following a schedule instead of each time a user logs in
